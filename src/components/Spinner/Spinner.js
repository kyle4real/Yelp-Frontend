import React from "react";

const Spinner = () => {
    return (
        <div className="spinner__container standard-container">
            <div className="spinner button is-loading is-large"></div>
        </div>
    );
};

export default Spinner;
